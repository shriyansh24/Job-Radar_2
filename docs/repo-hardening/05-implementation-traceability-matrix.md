# Implementation Traceability Matrix

## Purpose
Show what P0, P1, and P2 were supposed to deliver, what is actually present now, and what should be ported, hardened, deferred, or rejected.

## Source-Of-Truth Status
- Status: `DOCUMENTED_WORKING_SET`
- Scope: phase and branch traceability for repository hardening
- Last validation basis: GitHub PR metadata for PRs `#12`, `#13`, `#17`, local branch history, and direct file inspection on `2026-03-27`

## Interpretation Rules
- `main` is treated as the stable merged backend/P2 baseline.
- `codex/ui-changes` is treated as the live UI and current workspace baseline.
- `feat/p1-core-value` is treated as a selective port source, not as a merge candidate.
- A capability is only marked `shipped` when the code exists on the current branch tip, not because a PR body claimed it.

## Matrix

| Capability | Intended phase / source | Backend | Frontend | DB / migrations | Workers / infra | Test evidence | Doc evidence | Current branch locus | Disposition | Required action |
|---|---|---|---|---|---|---|---|---|---|---|
| ATS IDs and composite dedup keys | P0 / PR `#12` | `partial` on `codex/ui-changes` via `jobs/models.py`, scraper ATS identity extraction, and dedup-aware persistence in `scraping/service.py` | `absent` as a first-class UI concept | `partial` via `20260327_add_job_ats_identity.py` on `codex/ui-changes` | `n/a` | `backend/tests/unit/search/test_ats_composite_key.py`, `backend/tests/integration/scraping/test_scraping_identity.py`, and `backend/tests/migrations/test_job_ats_identity_migration.py` | `partial` | selectively ported into `codex/ui-changes` | `HARDEN` | Keep the current ATS identity path, then extend it into richer dedup/search/product follow-through only where the current architecture still benefits. |
| Company/title/location normalization | P0 / PR `#12` | `partial` on `codex/ui-changes` via `backend/app/scraping/normalization.py` plus normalization-aware dedup wiring in `deduplication.py` | `absent` as a first-class UI surface | `n/a` | `n/a` | `backend/tests/unit/search/test_normalization.py`, `backend/tests/unit/scraping/test_scraper_normalization.py`, and `backend/tests/unit/dedup/test_deduplication.py` | `partial` | selectively ported into `codex/ui-changes` | `HARDEN` | Keep the normalization layer in the live dedup/search path and extend it only where scraper identity quality still benefits. |
| Resume IR parsing | P0 / PR `#12` | `partial` on `codex/ui-changes` via `backend/app/resume/parser.py`, `ir_schema.py`, and upload wiring in `service.py` | `partial` because the current UI reuses the existing upload surface but does not expose structured IR directly | `partial` because the current branch persists `parsed_structured` but does not port the old tailoring-session migration family | `n/a` | `backend/tests/unit/resume/test_resume_parser.py` and `backend/tests/unit/resume/test_resume_service_parser.py` | `partial` | selectively ported from `feat/p1-core-value` into `codex/ui-changes` | `HARDEN` | Keep the upload parser/IR path and add UI/operator visibility only where it improves the current resume flow. |
| SQL pattern detector | P0 / PR `#12` | `shipped` on `codex/ui-changes` via `backend/app/analytics/pattern_detector.py` and the live analytics patterns endpoint | `shipped` via the analytics patterns panel on the current branch | `n/a` | `n/a` | `frontend/src/tests/pages/Analytics.page.test.tsx` | `partial` | selectively ported into `codex/ui-changes` | `HARDEN` | Keep the live analytics pattern surface and add deeper backend coverage only if this signal becomes operationally critical. |
| Local-first / hybrid model router | P0 / PR `#12` | `absent` on current branches; implementation exists on `feat/p1-core-value` | `absent` | `n/a` | `n/a` | branch-only local-router coverage on `feat/p1-core-value` | `research_only` | branch-only on `feat/p1-core-value` | `ARCHIVE` | Keep the current cloud-first/router stack on `codex/ui-changes`; treat the branch-era local-first router as historical research rather than committed live scope. |
| Freshness scoring | P0 / PR `#12` | `partial` on `codex/ui-changes` via `backend/app/enrichment/freshness.py` and `backend/app/jobs/schemas.py` | `partial` via the live Job Board row/detail freshness presentation on `codex/ui-changes` | `n/a` | `n/a` | `backend/tests/unit/search/test_freshness.py` plus freshness assertions in `backend/tests/integration/test_jobs_api.py` | `partial` | selectively ported into `codex/ui-changes` | `HARDEN` | Keep freshness in the ranking/display path and add stronger cross-route/operator assertions if it becomes a decision driver. |
| Six-stage dedup pipeline | P1 / PR `#13` | `partial` via enhanced dedup logic on `feat/p1-core-value` | `absent` | `n/a` | `n/a` | branch-only multi-stage dedup coverage on `feat/p1-core-value` | `underdocumented` | branch-only on `feat/p1-core-value` | `ARCHIVE` | The live branch already ships normalization, ATS identity, hybrid search, and freshness on the current scraper architecture. Keep the branch-era six-stage variant as historical reference rather than a live recovery target. |
| Embedding upgrade and hybrid search | P1 / PR `#13` | `partial` on `codex/ui-changes` via `backend/app/search/hybrid.py`, the live `EmbeddingService`, and semantic ranking in `backend/app/jobs/service.py` | `partial` via semantic mode in Job Board and Search Expansion handoff | `partial` because the embedding/search-vector migration lineage is still historical and not reworked in this batch | `partial` because the live queue runtime owns enrichment/embedding lanes but backfill policy is still thin | `backend/tests/unit/search/test_hybrid_search.py`, `backend/tests/integration/test_jobs_api.py`, and `frontend/e2e/flows/interview-search-recovered.spec.ts` | `partial` | selectively ported into `codex/ui-changes` | `HARDEN` | Treat hybrid search as live but still bounded: keep the current ranking path, then revisit migrations/backfill strategy separately instead of reviving the old branch wholesale. |
| Resume tailoring, rendering, and ATS validation | P1 / PR `#13` | `shipped` on `codex/ui-changes` because tailoring, ATS validation, council review, cover-letter generation, renderer/template modules, and backend-backed preview/export endpoints are live | `shipped` because the current UI ships upload, preview, export, versions, tailoring, and council surfaces | `shipped` for the committed live flow; branch-era tailoring-session persistence remains historical | `n/a` | `backend/tests/unit/resume/test_ats_validator.py`, `backend/tests/unit/resume/test_resume_parser.py`, `backend/tests/unit/resume/test_resume_service_parser.py`, `backend/tests/unit/resume/test_resume_templates.py`, `backend/tests/unit/resume/test_resume_service_preview_export.py`, `backend/tests/integration/resume/test_resume_api.py`, `frontend/src/tests/pages/ResumeBuilder.page.test.tsx`, and `frontend/e2e/flows/resume-template-preview.spec.ts` | `current_state` | live on `codex/ui-changes` | `SHIPPED` | Keep the current resume family as the committed flow and archive the older proposal/session model as historical complexity that was intentionally not promoted. |
| Form extraction and field mapping | P1 / PR `#13` | `partial` on `codex/ui-changes` via `backend/app/auto_apply/form_extractor.py`, `field_mapper.py`, `form_learning.py`, live orchestration/service wiring, and operator API endpoints | `partial` via the live Auto Apply operator controls for refresh, run-now, and pause | `n/a` | `partial` via `backend/app/workers/auto_apply_worker.py` batch execution | current `backend/tests/unit/auto_apply/test_form_extractor.py`, `backend/tests/unit/auto_apply/test_form_learning.py`, `backend/tests/unit/auto_apply/test_auto_apply_service.py`, `backend/tests/unit/auto_apply/test_auto_apply_orchestrator.py`, `backend/tests/workers/auto_apply/test_auto_apply_worker.py`, and `backend/tests/integration/auto_apply/test_auto_apply_api.py` | `partial` | mixed: recovered on `codex/ui-changes` with broader operator depth still branch-only on `feat/p1-core-value` | `HARDEN` | Keep the now-live operator/API path and add deeper workflow/browser coverage before treating auto-apply as shipped end to end. |
| Lever/Greenhouse adapters and safety layer | P1 / PR `#13` | `partial` on `codex/ui-changes` via `greenhouse_adapter.py`, `lever_adapter.py`, `safety.py`, and the now-live orchestrator/service path | `absent` | `n/a` | `partial` via worker-triggered batch execution | current `backend/tests/unit/auto_apply/test_greenhouse_adapter.py`, `backend/tests/unit/auto_apply/test_lever_adapter.py`, `backend/tests/unit/auto_apply/test_safety_layer.py`, `backend/tests/unit/auto_apply/test_auto_apply_service.py`, `backend/tests/unit/auto_apply/test_auto_apply_orchestrator.py`, and contract coverage in `backend/tests/contracts/providers/test_greenhouse_contract.py` and `backend/tests/contracts/providers/test_lever_contract.py` | `partial` | mixed: recovered on `codex/ui-changes` with deeper operator/coverage work still remaining | `HARDEN` | Keep the live execution path, then add integration coverage and operator-facing UI follow-through before claiming product-level completion. |
| Outcomes tracking | P1 / PR `#13` | `shipped` on current branches | `shipped` on current branches | `shipped` | `partial` because follow-on worker/search integrations are not explicit | current route/API coverage plus branch-only outcomes unit coverage on `feat/p1-core-value` | `current_state` | `main` and `codex/ui-changes` | `HARDEN` | Keep current shipped outcomes surfaces and document any remaining worker/search follow-through. |
| Interview prep engine | P1 / PR `#13` | `shipped` on `codex/ui-changes` via richer prompts, stage-aware prep, company/role context, and persisted interview sessions in `backend/app/interview/*` | `shipped` via the live prepare/history/practice tabs and recovered prep bundle panels on `codex/ui-changes` | `n/a` | `n/a` | `backend/tests/unit/interview/test_interview_service.py`, `backend/tests/unit/interview/test_interview_contextual_service.py`, `frontend/src/tests/pages/InterviewPrep.page.test.tsx`, and `frontend/e2e/flows/interview-search-recovered.spec.ts` | `current_state` | live on `codex/ui-changes` | `SHIPPED` | Keep the current prep flow and archive the branch-era prep-package persistence model as a historical alternative rather than a live gap. |
| Pipeline drag/drop ergonomics | P1 / PR `#13` | `n/a` | `partial` on `codex/ui-changes` via bounded drag/drop transitions, keyboard/pointer DnD support, and explicit `rejected` / `withdrawn` board stages | `n/a` | `n/a` | `frontend/src/tests/pages/Pipeline.page.test.tsx` plus `frontend/e2e/flows/route-family-outcomes.spec.ts` | `partial` | selectively ported into `codex/ui-changes` | `HARDEN` | Keep the current board ergonomics and only revive additional branch-era interactions if they still fit the current workflow contract. |
| Email module | P2 / PR `#17` | `shipped` on `main` and `codex/ui-changes` | `shipped` | `shipped` | `n/a` | `backend/tests/unit/email/test_email_parser.py` on `main` | `current_state` | `main` and `codex/ui-changes` | `HARDEN` | Add clearer taxonomy and observability notes; no recovery work required. |
| Networking module | P2 / PR `#17` | `shipped` on `main` and `codex/ui-changes` | `shipped` | `shipped` | `n/a` | `backend/tests/unit/networking/test_networking.py` on `main` | `current_state` | `main` and `codex/ui-changes` | `HARDEN` | Keep and improve taxonomy/docs. |
| Analytics predictor / RAG / GPU acceleration | P2 / PR `#17` | `shipped` on `main` and `codex/ui-changes` | `shipped` via analytics UI surfaces | `n/a` | `partial` because the worker/runtime expectations are still lightly documented | `backend/tests/unit/intelligence/test_ml_predictor.py`, `backend/tests/unit/intelligence/test_rag_pipeline.py`, `backend/tests/unit/intelligence/test_gpu_accelerator.py` on `main` | `partial` | `main` and `codex/ui-changes` | `HARDEN` | Document limits and coverage boundaries instead of treating the stack as fully production-complete. |
| Form learning and Workday adapter | P2 / PR `#17` | `shipped` on `main` and `codex/ui-changes` | `shipped` indirectly through current auto-apply surfaces | `shipped` | `n/a` | `backend/tests/unit/auto_apply/test_form_learning.py`, `backend/tests/unit/auto_apply/test_workday_adapter.py` on `main` | `partial` | `main` and `codex/ui-changes` | `HARDEN` | Align docs and tests with the live backend reality. |
| Salary intelligence | P2 / PR `#17` | `shipped` on `main` and `codex/ui-changes` | `shipped` on current branches | `shipped` | `n/a` | `backend/tests/unit/salary/test_salary_intel.py` on `main` | `current_state` | `main` and `codex/ui-changes` | `HARDEN` | Keep shipped surface and close remaining coverage/documentation gaps. |
| Dedup feedback | P2 / PR `#17` | `shipped` on `main` and `codex/ui-changes` | `absent` as a first-class UI feature | `shipped` | `n/a` | `backend/tests/unit/dedup/test_dedup_feedback.py` on `main` | `research_to_current` | `main` and `codex/ui-changes` | `HARDEN` | Keep and clarify how it fits the current dedup roadmap. |

## Archived Or Explicitly Rejected Branch-Era Variants
1. The branch-era local-first hybrid router remains research/history and is not part of the committed live runtime.
2. The branch-era six-stage dedup variant is superseded by the current ATS identity + normalization + freshness + hybrid-search path.
3. The branch-era resume proposal/session approval model is archived in favor of the current direct tailor + preview/export + council flow.
4. The branch-era interview prep-package persistence model is archived in favor of the current interview session + prep response flow.

## Explicit Non-Decisions
- This matrix does not authorize a blind merge of `feat/p1-core-value`.
- This matrix does not promote `docs/research/` into shipped scope by itself.
- This matrix does not treat PR body claims as proof unless the file-level evidence exists on a branch tip.
